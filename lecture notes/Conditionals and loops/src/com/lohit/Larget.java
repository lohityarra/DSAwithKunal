package com.lohit;

import java.util.Scanner;

public class Larget {
    public static void main(String[] args) {
        // take one number as max and iterate using if else

        //other wise use math class
        // example Math.max(c, Math.max(a,b))
    }
}
